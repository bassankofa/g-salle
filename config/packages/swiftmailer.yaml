swiftmailer:
  #url: "%env(MAILER_URL)%"
  #spool: { type: "memory" }
  transport: smtp
  auth_mode: login
  port: 587
  host: ssl0.ovh.net
  username: emma.gomis@adsglobalcorp.com
  password: "@dsgroup2022"
  spool: { type: "memory" }
